# ElectraKart Frontend Production Template
# In production, demo fallback MUST be false so users are never misled by local stale data
VITE_API_BASE_URL=https://api.electrakart.com/api/v1
VITE_ALLOW_DEMO_FALLBACK=false
